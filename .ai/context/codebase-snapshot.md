# Codebase snapshot

**Date:** 2026-09-01  
**State:** pre-implementation. No `src/`, extension `package.json`, or `media/`.

## What is in the repo

- `README.md` — product description and publishing (English)
- `LICENSE` — MIT (Copyright 2026 Łukasz Zileiński)
- `.ai/context/` — AI context
- `.cursor/rules/`, `.cursor/commands/` — agent rules and commands

## What is missing

Extension host, status bar, webview, Vitest tests, esbuild, VSIX.

## After the first code skeleton

Update this file: `src/` and `media/` trees, `package.json` scripts, known gaps vs PRD §13.
