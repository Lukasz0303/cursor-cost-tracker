# Source and tooling — VSIX ships dist/ + media/, not TypeScript
src/**
test/**
esbuild.mjs
tsconfig.json
vitest.config.ts
.gitignore
.gitattributes

# Agent / editor / CI meta
.ai/**
.cursor/**
.vscode/**
.github/**
AGENTS.md
release-notes/**

# Dependencies: JS is bundled; sql.js wasm is copied into dist/
node_modules/**

# Maps (watch/F5 only)
**/*.map
**/*.ts

# Local artifacts
**/*.vsix
_vsix*
.vscode-test/**
coverage/**

# Scratch assets that are not the marketplace icon
ChatGPT Image*

# README screenshots load from GitHub raw after vsce rewrite — keep them out of the VSIX
screenshots/**
